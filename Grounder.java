import java.io.*;
import java.util.*;
import java.util.regex.*;
import java.nio.file.*;

class Grounder
{

   public static void main(String[] args)
   {
      String testInput = ""+
      "party(sherwood).\n"+
      "party(walker).\n"+
      "condition(barren).\n"+
      "hold(pregnant).\n"+
      "in(rescind(X)):-mutualmistake(Lambda),violatebasicassumption(),"+
      "not risk(X),party(X),condition(Lambda).\n"+
      "mutualmistake(Lambda):-hold(Omega),call(union(ck,Omega),
      Grounder g = new Grounder();
      module.displayResults();
   }

}
